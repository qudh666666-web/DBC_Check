CAN矩阵-DBC一致性检查工具 v3.0.2

本版变化：
1. E2EDataLength 缺失或<=0时，自动给出“候选建议值”。
2. 报文级建议按 DLC(Byte) × 8 计算整帧候选长度，例如 DLC=64 时建议候选值为 512 bit。
3. 若报文存在 SignalGroup，提示不要机械采用整帧值；应按实际 E2E 保护区/SignalGroup 序列化长度确认。
4. Vector手册规则和DBC自身检查两处都会显示该建议。
5. 该建议只用于快速定位，不替代具体 E2E Profile/AUTOSAR规范或项目配置要求。
