v3.0.9

- 增加 DBC 报文/信号命名设置、全量预览、JSON配置和原名到新名映射。
- 命名中的 CAN ID 自动来自所属 BO_，统一使用 0x 前缀和大写十六进制，不写入扩展帧存储标志。
- 按标准帧/扩展帧身份区分相同数值的 CAN ID；同步修改 SG_、CM_ SG_、BA_ SG_、VAL_、SIG_GROUP_、SIG_VALTYPE_、SG_MUL_VAL_ 引用。
- 增加已明确引用真实节点的 BU_ 补全入口；Vector__XXX 不会被补入。
- 无 tkinter 时允许离线核心解析和自测导入；启动图形界面仍会明确提示环境限制。
